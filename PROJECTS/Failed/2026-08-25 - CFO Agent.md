# PRD — CFO Agent

**Date:** 2026-08-25
**Requested by:** Eikko
**Status:** 🟡 **Phase 1 delivered · Phase 2 blocked awaiting Eikko's sign-off**

> ## ⚠️ THIS IS A RECONSTRUCTION, NOT THE APPROVED PRD
>
> **The approved spec — `PROJECTS/DRAFT PRD - 2026-08-25 - CFO Agent.md` — does not exist and never has.** It is absent from the working tree and from every commit in `git log --all`. So are the two PRDs the build brief cites as precedent (`DRAFT PRD - 2026-08-25 - Chief of Staff Orchestrator.md`, `PRD - CMO Agent System (DRAFT).md`) and the rename PRD (`DRAFT PRD - 2026-08-25 - Starfix to Sellervate Rename.md`). No `cmo.md` or `chief-of-staff.md` agent exists either.
>
> **This file was written by the builder from the build instruction**, so that a build log has somewhere to live per the Builder Pipeline rules. It is **not** Eikko's signed-off spec, it does **not** carry the "Decisions locked" table the brief refers to, and it must not be mistaken for the contract. If the real PRD surfaces and disagrees with anything built, **the PRD wins** — every Phase 1 artefact is a plain markdown file under git and is cheap to amend.
>
> **Filed in `Failed/` per `PROJECTS/README - Builder Pipeline.md`**, which routes anything needing sign-off on a destructive step here. That is the pipeline's label for *blocked, needs Eikko* — it is not a statement that the build failed. Phase 1 is complete and verified.

## Problem

`billing-auditor` had never produced a real financial report. Its rollup files were unexecuted templates carrying illustrative placeholder numbers ("Chris Caffera: 80 hrs logged", "$560") that read as real and had been sitting in the repo since August 2026. Meanwhile the live financial data — income, bills, ₱340,672 of debt — lived in Notion with no agent able to read it, and expenses were trapped in a pipe-delimited free-text field Notion cannot sum.

## Success criteria

- [x] A financial domain specialist exists, scoped to money only, with no global routing.
- [x] The rate card and `billing-auditor`'s known-gaps audit survive its deletion.
- [x] A real monthly close comes out of live Notion data, not a template.
- [x] Figures reconcile by hand against Notion, row by row, and discrepancies are reported rather than adjusted.
- [x] The agent refuses to fabricate an hours figure when asked directly.
- [ ] Expenses are queryable rather than free text. *(gated)*
- [ ] Eikko's real tool spend is tracked. *(gated)*

## Scope — Phase 1 (delivered)

`.claude/agents/cfo.md` · four report formats · `.claude/commands/cfo.md` · Darius profile + EOD log · FX rate block in `Important info.md` · roster updates.

## Scope — Phase 2 (gated, not performed)

Tools & Subscriptions data source · Claude row correction · `Bills` → linked `Expenses` migration · `billing-auditor.md` deletion.

## Non-goals

No global routing table, no root `CLAUDE.md` routing block (that is `chief-of-staff`'s). No income target. No Starfix→Sellervate rename. No client-tool cost tracking. **No financial actions of any kind.**

## Constraints

Notion is source of truth for money; `OUTPUT/Monthly Reports/` for reports, never synced back. FX read from `Important info.md`, never looked up live. `_shared/connector-status.md` before claiming any tool is live. Never invent a number.

---

# BUILD LOG — 2026-08-25

## Delivered (Phase 1) — all local, all reversible under git

| File | |
|---|---|
| `.claude/agents/cfo.md` | New. Rate card, verbatim inherited audit + dated re-verification, FX rule, dual ledger, sync protocol, four report formats. |
| `.claude/commands/cfo.md` | New. |
| `CLIENT PROFILES/Darius - Profile.md` | New. Every engagement field `TBD — confirm with Eikko`. |
| `OUTPUT/End-of-Day Reports/Darius - End of Day Log.md` | New, empty. |
| `CLIENT PROFILES/Important info.md` | `## FX Rate` block added, values intentionally blank. |
| `README.md` · `CLAUDE.md` · `.claude/agents/README.md` · `.claude/commands/agent-manager.md` | Darius + `cfo` added to rosters. `billing-auditor` retained, marked superseded — **it has not been deleted.** |
| `OUTPUT/Monthly Reports/2026-08 - Monthly Close.md` | First real close ever produced from this system. |
| `OUTPUT/Monthly Reports/Cash Flow - Rolling 60 Day.md` | |
| `OUTPUT/Monthly Reports/Debt Payoff Plan.md` | Marked `PROVISIONAL`. |
| `OUTPUT/Monthly Reports/_backup/2026-08-25 Finance Tracker (pre-migration).md` | Verbatim export of all 9 Finance Tracker + 19 Bills rows, taken before proposing any Notion write. |

## Reconciliation

| Measure | Expected | Live | |
|---|---|---|---|
| Fixed monthly obligations | ₱49,806 | ₱49,806 | ✅ 18/19 rows. Confirmed twice — direct sum, and as the September total in the runway ledger. |
| Total outstanding debt | ₱340,672 | ₱340,672 | ✅ 16/19 rows. |
| August income received | ₱54,570 (6/9) | **₱37,590 (5/9)** | ❌ short ₱16,980, one row. |

**Not adjusted to fit.** The entire gap is the Yoni 2026-08-25 row (`3c2811e21c7f80b5beb9ddd7e6309461`): its `Income` is now empty and its `Bills` string reads `Homecredit Laptop - 5222 | Dongki and Nanay Allowance - 1000` — the `Claude Max Plan - 8000` entry the build was meant to correct is gone. `Claude` appears in none of the 9 `Bills` values. Edited between the 2026-08-25 verification and this run.

## Verification

- **Fabrication test — PASSED.** Asked for Chris Caffera's August billable hours "for an invoice." Answered "Not logged," produced no number, refused to derive one from contracted hours, task counts, meeting durations, or the existing placeholder. Also surfaced the `80 hrs / $560` landmine unprompted and warned against invoicing it.
- **Pending rows — confirmed.** Chris Caffera 08-28, Chris Drew 08-26, Yoni 08-31 report as *pending*, not ₱0. **A fourth pending row exists that the brief did not list: Yoni 08-25** — the discrepancy row.
- **Bills parsing — reconciled.** All 6 non-empty strings re-sum exactly (₱51,354). Zero failures.

## Blocked — needs Eikko

1. Tools & Subscriptions data source — needs his full subscription list first.
2. Claude row correction — **target data no longer exists as described.**
3. `Bills` → `Expenses` migration — backup taken; parse table needs approval; 6 ambiguous entries need categories.
4. `billing-auditor.md` deletion.

## Found, not asked about

- **The `80 hrs` placeholder is live in three files**, two of them inside `OUTPUT/Monthly Reports/` where `cfo` now writes real reports.
- **No opening cash balance is recorded anywhere** — runway is flow, not position.
- **September has ₱49,806 of dated obligations and zero income rows.**
- Yoni's rate is a live mismatch (`$5/hr` profile vs `(TBD)` in `Important info.md`); the two mismatches `billing-auditor` reported have since been fixed and its claim is stale.
- `connector-status.md` names the Notion workspace "eikko mae ybanez's Space"; it is **"WORK Command Center."**

---

**Sign-off:** ⬜ Approved to build — Eikko *(Phase 1 built from the build instruction in the PRD's absence; Phase 2 awaiting explicit go-ahead.)*
